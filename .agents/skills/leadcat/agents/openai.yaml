interface:
  display_name: "Leadcat"
  short_description: "Repo-specific patterns and workflows for leadcat"
  default_prompt: "Use the leadcat repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true